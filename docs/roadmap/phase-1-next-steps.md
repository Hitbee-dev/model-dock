# Phase 1 Next Steps

The initial skeleton is intentionally small. Exact follow-up work:

1. Add mock and live LiteLLM integration tests.
2. Wire chat streaming and RAG context injection into the API/browser flow.
3. Add real environment loading from `.env`.
4. Add database migrations for required domains.
5. Add production Docker Compose override with generated secret checks.
6. Add rendered docs site with JSON-LD and OpenGraph assets.

Last updated: 2026-05-03
