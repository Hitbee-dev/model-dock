# Phase 1 Next Steps

The initial skeleton is intentionally small. Exact follow-up work:

1. Replace placeholder HTTP servers with the chosen web/API frameworks.
2. Add real environment loading from `.env`.
3. Add database migrations for required domains.
4. Add live LiteLLM integration tests behind opt-in environment variables.
5. Add production Docker Compose override with generated secret checks.
6. Add rendered docs site with JSON-LD and OpenGraph assets.

Last updated: 2026-05-02
