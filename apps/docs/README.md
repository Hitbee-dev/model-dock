# ModelDock Docs Site

Static documentation renderer for public ModelDock docs.

```bash
pnpm --filter @modeldock/docs build
```

The renderer reads root Markdown docs and writes HTML plus crawler metadata into `apps/docs/dist`.
